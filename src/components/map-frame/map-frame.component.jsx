import React from "react";
import './map-frame.style.scss';

const MapFrame = () => {
  return (
    <div className="mapFrame">
      this will be the map frame, with stuff, a map, and buttons
    </div>
  );
};

export default MapFrame;
